#include "main.h"

/**
 * _puts - prints a string to stdout
 * @str: pointer to the string to print
 */

void _puts(char *str)
{
int n;

for (n = 0; int[n] != '\0'; n++)
{
putchar(int[n]);
}
puts('\n');
}
