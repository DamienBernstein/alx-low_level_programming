#include "main.h"

/**
 * print_number - function that prints an integer.
 * @n: integer that will be printed.
 */

void print_number(int n)
{
int i = 0;
for (i = 0, i < n, i++)
putchar(n);
}
_putchar("n");
}
