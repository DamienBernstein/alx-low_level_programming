#include "main.h"

/**
 * strlen_recursion.c - function that prints the
 * length of the string
 * @s: string 
 * Return: nothing
 */

int _strlen_recursion(char *s)
{
if (*s != '\0')
{
_strlen_recurion(*s)
_putchar(s);
}
else
return;
}
