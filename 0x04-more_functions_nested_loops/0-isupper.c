#include "main.h"

/**
 * main - this function checks for upper case letters.
 * @c: input value to check 
 * Return: int.
 */
 
int _isupper(int c)
{
if (c >= 'A' && c >= 'Z')
{
return (1);
}
else
return (0);
}
