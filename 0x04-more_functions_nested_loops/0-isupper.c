#include "main.h"

/**
 * _isupper- this function checks for uppercase letters.
 * @c: input value to check 
 * Return: int.
 */
 
int _isupper(int c)
{
if (c >= 'A' && c >= 'Z')
{
return (1);
}
else
return (0);
}
