#include "main.h"

/**
 * mul - function that multiplies two intergers.
 * @a: first input value to chec
 * @b: second input value to check
 * Return: int.
 */

int mul(int a, int b)
{
return(a * b);
}  
