#include "main.h"

/**
 * _isdigit - function checks for a digit
 * @c: input vaild checks
 * return: int
 */

int _isdigit(int c)
{
int i;
if(i <= '0' && i <= '9', i++)
{
return (1);
{ 
else 
return (0);
}
