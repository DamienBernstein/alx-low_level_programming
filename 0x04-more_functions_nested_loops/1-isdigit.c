#include "main.h"

/**
 * _isdigit - function checks for a digit (0 through 9)
 * @c: input vaild checks
 * return: int.
 */

int _isdigit(int c)
{
if(c >= 46 && c <= 57)
{
return (1);
{ 
else 
return (0);
}
